{
  "name": "neon-runner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based neon endless runner rendered with PlayCanvas (world) and Three.js (player overlay).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.3",
    "@supabase/supabase-js": "^2.105.4",
    "playcanvas": "^2.0.0",
    "three": "^0.164.1"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
