{
  "name": "orbs-runner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ORBS Runner — browser-based neon endless runner (PlayCanvas world + Three.js avatar overlay).",
  "scripts": {
    "dev": "vite",
    "favicon": "node scripts/embed-favicon-svg.mjs",
    "prebuild": "node scripts/check-vercel-supabase-env.mjs && node scripts/embed-favicon-svg.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.3",
    "@supabase/supabase-js": "^2.105.4",
    "playcanvas": "^2.0.0",
    "three": "^0.164.1"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
